# Keep LF in the working tree on every platform (Prettier enforces LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.jpg binary
*.webp binary
*.woff binary
*.woff2 binary
